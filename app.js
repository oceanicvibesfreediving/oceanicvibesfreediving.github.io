const defaultContent={heroEyebrow:'Riviera Maya · Mexico',heroTitle:'Master|your depth.',heroSubtitle:'Elite freediving instruction, advanced equalization, and depth training for people who want to feel at home beneath the surface.',aboutLead:'At OceanicVibes, we train athletes, watermen, and dedicated individuals to safely unlock their true aquatic potential.',aboutBody:'From Frenzel and Mouthfill equalization to CO2 tolerance and deep relaxation, every session is built around calm, measurable progression and uncompromising safety.',footerCopy:'Private instruction and depth training in the waters of the Yucatan.',footerEmail:'train@oceanicvibes.com',instagram:'@oceanic__vibesfreediving',courses:[{date:'Oct 15–17',location:'Playa del Carmen',title:'AIDA 2 Foundation',description:'Build a bulletproof foundation with Frenzel equalization, duck dives, breathing techniques, and safety protocols up to 20 meters.'},{date:'Nov 02–05',location:'Bacalar',title:'Stillness & Technique',description:'A focused Bacalar weekend for breathwork, relaxation, body position, and the calm precision that makes depth feel effortless.'},{date:'Nov 18–22',location:'Playa del Carmen',title:'Advanced Depth',description:'Line training in Free Immersion and Constant Weight, with advanced equalization and coaching through your next depth barrier.'}]};
function getContent(){return fetch('content.json',{cache:'no-store'}).then(response=>response.ok?response.json():Promise.reject()).catch(()=>defaultContent)}
function escapeHtml(value){return String(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}
getContent().then(content=>{const put=(id,value)=>{const element=document.getElementById(id);if(element)element.textContent=value};put('heroEyebrow',content.heroEyebrow);document.getElementById('heroTitle').innerHTML=content.heroTitle.split('|').map((part,index)=>index?`<i>${escapeHtml(part)}</i>`:escapeHtml(part)).join('<br>');put('heroSubtitle',content.heroSubtitle);put('aboutLead',content.aboutLead);put('aboutBody',content.aboutBody);put('footerCopy',content.footerCopy);put('footerEmail',content.footerEmail);document.getElementById('footerEmail').href=`mailto:${encodeURIComponent(content.footerEmail)}`;put('instagramLink',content.instagram);document.getElementById('instagramLink').href=`https://instagram.com/${encodeURIComponent(content.instagram.replace('@',''))}`;
const list=document.getElementById('courseList');let activeFilter='all';function renderCourses(){const courses=content.courses.filter(course=>activeFilter==='all'||course.location===activeFilter);list.innerHTML=courses.length?courses.map(course=>`<article class="course-card reveal is-visible"><div class="course-date">${escapeHtml(course.date)}<span class="course-location">${escapeHtml(course.location)}</span></div><div><h3>${escapeHtml(course.title)}</h3><p>${escapeHtml(course.description)}</p></div><span class="course-arrow">↗</span></article>`).join(''):'<div class="empty-state">No sessions are listed for this location yet.</div>'}renderCourses();document.querySelectorAll('.filter-button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter-button').forEach(item=>item.classList.remove('is-active'));button.classList.add('is-active');activeFilter=button.dataset.filter;renderCourses()}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(element=>observer.observe(element))});
